{
	"name": "miti",
	"version": "1.0.0",
	"description": "An open source Nepali Calendar App with cross compatibility with google calendar",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"postinstall": "cd frontend && pnpm install && cd .. && cd backend && pnpm install && cd ..",
		"dev": "concurrently \"cd frontend && pnpm run dev\" \"cd backend && pnpm run dev\""
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/poskOfficial/Miti.git"
	},
	"keywords": ["Calendar", "Nepal-calendar", "PWA", "Nepal"],
	"author": "@ParajuliBkrm, @POSKOfficial",
	"license": "MIT",
	"bugs": {
		"url": "https://github.com/poskOfficial/Miti/issues"
	},
	"homepage": "https://github.com/poskOfficial/Miti#readme",
	"devDependencies": {
		"concurrently": "^8.2.0"
	}
}
